version: '2'
services:
  postgres:
    container_name: frontier_pg
    image: postgres
    ports:
      - "5432:5432"
  redis:
    container_name: frontier_redis
    image: redis
    ports:
      - "6379:6379"
